# Path to where the NextBus datastore saves and loads data
NEXTBUS_FILE_PATH = 'nextbus.data'

# regionTitle filter for getting data from agencies when building datastore
# e.g. Setting to 'California' will only get agencies in the California region
AGENCY_REGION = 'California'
